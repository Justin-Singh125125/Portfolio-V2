import { IntroductionBadge } from './components';

export const Hero = () => {
  return (
    <section className="h-screen flex items-center justify-center">
      <div>
        <IntroductionBadge />
      </div>
    </section>
  );
};
